# Server connection URL
CONNECTION_URL=http://localhost:4000

# Optional: Anthropic API key (can also be passed via client options)
# If using Claude Max via ~/.claude mount, this is not required
ANTHROPIC_API_KEY=sk-ant-your-api-key-here
